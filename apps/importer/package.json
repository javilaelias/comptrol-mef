{
  "name": "importer",
  "version": "0.0.1",
  "private": true,
  "description": "Offline import job (Excel) for Comptrol-MEF",
  "prisma": {
    "schema": "../api/prisma/schema.prisma"
  },
  "scripts": {
    "import:docs": "prisma generate --schema ../api/prisma/schema.prisma && ts-node --transpile-only scripts/import-docs.ts"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.5.0",
    "@prisma/client": "^7.5.0",
    "bcryptjs": "^3.0.3",
    "dotenv": "^17.2.3",
    "pg": "^8.20.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "prisma": "^7.5.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  }
}
