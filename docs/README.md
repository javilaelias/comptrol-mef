# docs/

Esta carpeta se usa para importar data local a la BD.

Por defecto, los archivos con datos reales (`.xls`, `.xlsx`, `.pdf`) están en `.gitignore` para evitar subir información sensible a GitHub.

Archivos esperados (local):
- `EQUIPOS TECNOLOGICOS.xlsx`
- `Relacion de Licencias y aplicativos MEF - 31 dic 2025.xls` (o `.converted.xlsx`)
- `Encuesta ENAD 2025[R].pdf`

Mantener versionado:
- `docs/xampp/` (snippets de configuración)

